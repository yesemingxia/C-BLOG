# 编译产物 / Build outputs
build/
build-clangd/
out/
x64/
Debug/
Release/

# C/C++
*.exe
*.obj
*.o
*.a
*.lib
*.so
*.dll
*.exp
*.pdb
*.ilk
*.idb

# CMake
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
compile_commands.json
CTestTestfile.cmake
Makefile

# Visual Studio
*.sln
*.vcxproj
*.vcxproj.filters
*.vcxproj.user
*.suo
*.user
*.userosscache
*.sdf
*.opensdf
.vs/

# 前端依赖与构建 / Frontend deps and build
node_modules/
frontend/dist/
source/frontend-source/node_modules/
source/frontend-source/dist/
source/frontend-source/auto-imports.d.ts
source/frontend-source/components.d.ts

# 前端日志与缓存
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.pnpm-debug.log*
*.log
*.tlog
*.lastbuildstate

# 包管理器锁文件（按项目约定保留其一即可）
package-lock.json
yarn.lock
pnpm-lock.yaml
bun.lock

# 本地配置 / 敏感信息 / Local and sensitive configs
config/config.json
config/config.local.json
config/config.*.local.json
.env
.env.local
.env.*.local

# Markdown 文档（如果不需要提交）
*.md

# IDE / 编辑器
.vscode/
.idea/
*.swp
*.swo
*~
.clangd

# 操作系统文件
.DS_Store
Thumbs.db
desktop.ini

# 数据库与运行时数据
mysql_data/
redis_data/
*.db
*.sqlite
*.sqlite3

# Generated style-transfer images
uploads/

# 媒体素材 —— 一律不入库
#
# 两个理由：
#   1. 体积（背景音乐 3.7MB、六个背景视频共 71MB）
#   2. **版权归属未明** —— 这些素材来自外部，入库会跟着仓库一起分发
#
# showcase 的静态资源落在 source/frontend-source/public/showcase/assets/，
# 走 .gitignore 排除后仍可正常本地运行（Vite 从磁盘读 public/，不经过 git）。
# 确有必要入库时用 `git add -f <file>` 显式覆盖。
#
# ⚠️ 注意不要在这里加 `*.ts` —— 那是 TypeScript 源码的扩展名。
#    同理 `*.map` 是 sourcemap，别顺手加。

# 音频
*.mp3
*.wav
*.m4a
*.aac
*.ogg
*.oga
*.opus
*.flac
*.aiff
*.aif
*.weba
*.mid
*.midi

# 视频
*.mp4
*.m4v
*.webm
*.mov
*.avi
*.mkv
*.wmv
*.flv
*.mpeg
*.mpg
*.3gp
*.ogv

# 压缩包 / 临时文件
*.zip
*.tar.gz
*.tar
*.rar
*.7z
tmp/
temp/

# ============================================================================
# 本地 / 工具 / 第三方产物 —— 不属于项目源码，一律不入库
# ============================================================================

# Agent 与工具的工作区（含对话记忆、验证截图、临时脚本、桌面状态）
.workbuddy/
.reasonix/
reasonix.toml

# 编辑器本地配置（含本机绝对路径）
CMakeSettings.json
CMakeUserPresets.json

# 本地备份快照（60+ 文件的整份前端拷贝，需要时从磁盘取即可）
backups/

# 从市场下载的第三方 Skill 素材（版权归属第三方，不入库）
gathered-scenes-zine-skill/

# 前端构建产物校验目录（临时，不入库）
dist-check-fx/
